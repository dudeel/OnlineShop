name: online_shop
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_native_splash: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter_native_splash:
  image: assets/loading_screen_logo.png
  color: "#FFFFFF"

flutter:
  uses-material-design: true
  assets:
    - assets/
